#include<stdio.h>
int main()
{
	char ch[100],i,c=0;
	printf("enter the character");
	//scanf("%s",ch);
	gets(ch);
	//puts(ch);
	printf("%s",ch);
	for(i=0;ch[i]!='\0';i++)
	{
		
	}
	printf("the count is %d",i);
}
